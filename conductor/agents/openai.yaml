interface:
  display_name: "Conductor"
  short_description: "Autopilot context firewall for clean master sessions"
  default_prompt: "Use $conductor as an Autopilot context firewall: handle soft gates automatically, route specialized work outward, and ask the user only at hard gates."

policy:
  allow_implicit_invocation: true
