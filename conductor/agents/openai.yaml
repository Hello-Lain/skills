interface:
  display_name: "Conductor"
  short_description: "Autopilot context firewall and routing"
  default_prompt: "Use $conductor proactively as an Autopilot context firewall: route specialized work outward, require Handoff Capsules, and ask only at hard gates."

policy:
  allow_implicit_invocation: true
