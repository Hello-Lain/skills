interface:
  display_name: "Codex2Codex"
  short_description: "Meight loops with handoff capsules"
  default_prompt: "Use $codex2codex with a fresh MEIGHT_HOME per request and require worker Handoff Capsules for review, verification, or Decision Council work."

policy:
  allow_implicit_invocation: false
