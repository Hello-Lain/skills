interface:
  display_name: "Plan Grill"
  short_description: "Validated plans with execution handoff"
  default_prompt: "Use $plan-grill to harden a production plan.md, validate the contract, and include an Execution Handoff without executing it."
