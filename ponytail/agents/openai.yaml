interface:
  display_name: "Ponytail"
  short_description: "Lazy senior dev mode for minimal, behavior-preserving code"
  default_prompt: "Use $ponytail to choose the simplest behavior-preserving solution: YAGNI → stdlib → native → existing deps → one line → minimum."

policy:
  allow_implicit_invocation: true
