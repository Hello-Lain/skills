# Override this list with real VLMEvalKit benchmark names, for example:
# uv run torchrun --nproc_per_node=2 src/eval.py eval_tasks.datasets='[MMBench_DEV_EN]'
datasets:
  - MMBench_DEV_EN

dataset_kwargs: {}

# Passed to dataset.evaluate(result_file, **judge_kwargs) on rank 0.
# Keep API keys in environment variables, not in YAML.
judge: {}
