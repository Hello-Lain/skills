# Copy to configs/local/default.yaml for machine-specific overrides.
# This file is ignored by git and loaded with `optional local: default`.

paths:
  root_dir: ${oc.env:PROJECT_ROOT,.}
  data_dir: ${paths.root_dir}/data

# Example:
# model:
#   path: ~/models/qwen-vl
