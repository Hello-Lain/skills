interface:
  display_name: "Deprecation Migration"
  short_description: "Plan migrations, deprecations, and doc lifecycle cleanup"
  default_prompt: "Use $deprecation-and-migration for safe migration, deprecation, or doc cleanup."
policy:
  allow_implicit_invocation: true
